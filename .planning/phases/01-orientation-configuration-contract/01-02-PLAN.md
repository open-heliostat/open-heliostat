---
phase: 01-orientation-configuration-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/HeliostatService.h
  - src/HeliostatService.cpp
  - platformio.ini
  - test/test_orientation_contract.cpp
  - test/test_orientation_persistence.cpp
  - test/test_orientation_api_contract.cpp
autonomous: true
requirements:
  - CONF-01
  - ORNT-02
must_haves:
  truths:
    - Orientation values persist across reboot and load with level defaults when absent.
    - Combined orientation updates do not commit partial invalid values.
    - Contract behaviors are checked by repeatable automated tests instead of build-only validation.
  artifacts:
    - path: src/HeliostatService.h
      provides: Save-map inclusion for mountOrientation fields in persisted heliostat config.
    - path: test/test_orientation_contract.cpp
      provides: Automated clamp and atomic validation checks for orientation payloads.
    - path: test/test_orientation_persistence.cpp
      provides: Automated persistence and legacy-default coverage.
    - path: test/test_orientation_api_contract.cpp
      provides: Automated request and response contract checks for orientation fields.
  key_links:
    - from: src/HeliostatService.h
      to: /config/heliostat.json
      via: JsonSaveManager filtered save map
      pattern: root\["mountOrientation"\]
    - from: test/test_orientation_contract.cpp
      to: src/HeliostatService.cpp
      via: JsonRouter parse behavior assertions
      pattern: mountOrientation
---

<objective>
Complete persistence and verification for the orientation contract so behavior survives restart and can be validated quickly in execution loops.

Purpose: Deliver CONF-01 with explicit automated checks and close Wave 0 validation gaps for Phase 1.
Output: Persisted mountOrientation save-map wiring and orientation contract test harness/files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-orientation-configuration-contract/01-VALIDATION.md
@src/HeliostatService.h
@src/HeliostatService.cpp
@platformio.ini
@test/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist mountOrientation and preserve legacy defaults on load</name>
  <read_first>
    - .planning/phases/01-orientation-configuration-contract/01-CONTEXT.md
    - src/HeliostatService.h
    - src/HeliostatService.cpp
    - lib/framework/FSPersistence.h
  </read_first>
  <files>src/HeliostatService.h, src/HeliostatService.cpp</files>
  <behavior>
    - Missing mountOrientation in stored config resolves to tiltDeg=0.0 and tiltAzimuthDeg=0.0 at runtime.
    - Valid orientation updates mark persistence-relevant fields as changed.
    - Invalid combined orientation updates do not alter persisted orientation values.
  </behavior>
  <action>Add mountOrientation.tiltDeg and mountOrientation.tiltAzimuthDeg to getSaveMap and ensure defaults are materialized on load/read paths when fields are absent. Keep immediate persistence behavior after valid changes using existing save manager flow, and do not introduce a migration script.</action>
  <verify>
    <automated>pio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>Orientation keys are included in save map, legacy files without mountOrientation still produce level defaults, and persisted values reload correctly after restart flow.</acceptance_criteria>
  <done>CONF-01 storage behavior is implemented in existing heliostat persistence pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add orientation contract and persistence automated tests</name>
  <read_first>
    - .planning/phases/01-orientation-configuration-contract/01-VALIDATION.md
    - platformio.ini
    - test/
  </read_first>
  <files>platformio.ini, test/test_orientation_contract.cpp, test/test_orientation_persistence.cpp, test/test_orientation_api_contract.cpp</files>
  <behavior>
    - Test 1: mountOrientation patch updates only provided key and preserves other key.
    - Test 2: out-of-range tiltDeg and tiltAzimuthDeg values clamp to [-90,90] and [0,360].
    - Test 3: invalid mountOrientation payload fails atomically without partial orientation mutation.
    - Test 4: save-map includes orientation keys and legacy missing values default to zero.
  </behavior>
  <action>Create a runnable orientation test harness under test/ and add any required test environment entry in platformio.ini so pio test can execute quickly. Use explicit fixture payloads and expected values matching locked decisions. Keep tests deterministic and independent from real hardware where possible.</action>
  <verify>
    <automated>pio test -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>All new orientation test files compile and run under pio test with assertions covering clamp, patch, atomic reject, and persistence default behavior.</acceptance_criteria>
  <done>Wave 0 orientation validation gap is closed with repeatable automated tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run phase-level verification commands and document evidence in summary</name>
  <read_first>
    - .planning/phases/01-orientation-configuration-contract/01-VALIDATION.md
    - .planning/phases/01-orientation-configuration-contract/01-01-PLAN.md
  </read_first>
  <files>.planning/phases/01-orientation-configuration-contract/01-02-SUMMARY.md</files>
  <action>Execute the final verification loop with pio run and pio test, capture pass or fail outcomes with concrete command output snippets, and include requirement-to-test evidence mapping in the summary file generated by execute workflow.</action>
  <verify>
    <automated>pio run -e esp32-s3-devkitc-1 && pio test -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>Summary includes executed commands, outcomes, and explicit mapping that CONF-01 and ORNT-02 behaviors are validated.</acceptance_criteria>
  <done>Phase 1 verification evidence is ready for /gsd-verify-work.</done>
</task>

</tasks>

<verification>
- pio run succeeds for target environment.
- pio test executes new orientation tests and reports pass status.
- Persistence and legacy default behavior are demonstrated by tests and summary evidence.
</verification>

<success_criteria>
- CONF-01 satisfied by save-map persistence and load defaults.
- ORNT-02 reinforced by automated clamp and invalid payload tests.
- Phase 1 has build plus test validation loops ready for downstream execution.
</success_criteria>

<output>
After completion, create .planning/phases/01-orientation-configuration-contract/01-02-SUMMARY.md
</output>

---
phase: 01-orientation-configuration-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/heliostat.h
  - src/HeliostatService.h
  - src/HeliostatService.cpp
autonomous: true
requirements:
  - ORNT-01
  - ORNT-02
  - API-01
must_haves:
  truths:
    - User can set and read mount orientation in the main heliostat REST contract using tiltDeg and tiltAzimuthDeg.
    - Out-of-range degree values are clamped to documented bounds.
    - Invalid orientation payload shape returns a single generic validation error.
  artifacts:
    - path: src/heliostat.h
      provides: Heliostat orientation state fields with level defaults.
    - path: src/HeliostatService.cpp
      provides: mountOrientation parser/serializer with patch semantics and clamp behavior.
    - path: src/HeliostatService.h
      provides: router update/read wiring for heliostat-service REST/event flow.
  key_links:
    - from: src/HeliostatService.cpp
      to: src/heliostat.h
      via: HeliostatController mountOrientation state updates
      pattern: mountOrientation.*tiltDeg|tiltAzimuthDeg
    - from: src/HeliostatService.cpp
      to: /rest/heliostat
      via: HeliostatControllerJsonRouter parse and serialize
      pattern: JsonRouter<HeliostatController>
---

<objective>
Implement the orientation API contract in existing heliostat service flows so clients can read and patch mount orientation reliably.

Purpose: Deliver ORNT-01, ORNT-02, and API-01 with additive schema evolution and strict backward compatibility.
Output: Updated controller model plus REST/event parse and serialize behavior for mountOrientation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-orientation-configuration-contract/01-CONTEXT.md
@.planning/phases/01-orientation-configuration-contract/01-RESEARCH.md
@src/heliostat.h
@src/HeliostatService.h
@src/HeliostatService.cpp

<interfaces>
From src/heliostat.h:
- class HeliostatController stores controller state used by JsonRouter parse and serialize.

From src/HeliostatService.h:
- static StateUpdateResult update(JsonObject &root, HeliostatController &state, const String &originId)
- static const void getSaveMap(JsonObject &root, HeliostatController &state)

From src/HeliostatService.cpp:
- JsonRouter<HeliostatController> HeliostatControllerJsonRouter::router
- Existing parser handlers for nested object fields and patch semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add mount orientation state contract in controller model</name>
  <read_first>
    - .planning/phases/01-orientation-configuration-contract/01-CONTEXT.md
    - src/heliostat.h
  </read_first>
  <files>src/heliostat.h</files>
  <action>Add a dedicated mount orientation object or fields in HeliostatController with exact public API names tiltDeg and tiltAzimuthDeg in degrees. Set runtime defaults to level-mount values tiltDeg=0.0 and tiltAzimuthDeg=0.0. Keep schema additive only and do not move this state under sunTracker.</action>
  <verify>
    <automated>pio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>src/heliostat.h exposes orientation values with exact names tiltDeg and tiltAzimuthDeg and default initialization to 0.0 without changing existing fields.</acceptance_criteria>
  <done>Controller state supports orientation fields required by mountOrientation API contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement mountOrientation parse and serialize with clamp and patch semantics</name>
  <read_first>
    - .planning/phases/01-orientation-configuration-contract/01-CONTEXT.md
    - .planning/phases/01-orientation-configuration-contract/01-RESEARCH.md
    - src/HeliostatService.cpp
  </read_first>
  <files>src/HeliostatService.cpp</files>
  <action>Extend HeliostatControllerJsonRouter parse and serialize tables with a mountOrientation object containing tiltDeg and tiltAzimuthDeg. Use clamp policy exactly: tiltDeg in [-90, 90], tiltAzimuthDeg in [0, 360]. Implement object-level patch semantics so omitted mountOrientation keys preserve prior values. If mountOrientation is provided and any present field has invalid type or mountOrientation is not an object, reject the orientation update and return parser false for that branch so the write path can return generic validation error behavior.</action>
  <verify>
    <automated>pio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>POST payloads with mountOrientation update only provided keys; out-of-range numeric values clamp to bounds; malformed mountOrientation payload is rejected and no partial orientation field update occurs.</acceptance_criteria>
  <done>Router contract supports read/write orientation in main heliostat payload with deterministic validation behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire generic validation error behavior through existing heliostat REST pattern</name>
  <read_first>
    - src/HeliostatService.h
    - src/HeliostatService.cpp
    - src/lib/HttpStateRouterEndpoint.h
  </read_first>
  <files>src/HeliostatService.h, src/HeliostatService.cpp</files>
  <action>Adjust HeliostatControllerJsonRouter update return path so mountOrientation parse failures produce StateUpdateResult::ERROR while valid updates still classify as CHANGED when save-map fields are touched. Preserve existing /rest/heliostat and heliostat-service usage with no new endpoint. Ensure behavior remains backward compatible for legacy clients that do not send mountOrientation.</action>
  <verify>
    <automated>pio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>Invalid orientation writes map to HTTP 400 generic error path, valid orientation writes remain in existing /rest/heliostat flow, and payloads without orientation continue to behave unchanged.</acceptance_criteria>
  <done>API-01 and ORNT-02 error-path behavior are implemented via existing heliostat service patterns.</done>
</task>

</tasks>

<verification>
Build succeeds and contract checks confirm mountOrientation appears in GET /rest/heliostat and validates according to locked ranges and generic error policy.
</verification>

<success_criteria>
- ORNT-01 satisfied by read/write orientation fields in main heliostat contract.
- ORNT-02 satisfied by clamp bounds and reject malformed orientation object behavior.
- API-01 satisfied by existing /rest/heliostat and heliostat-service flow support with no new endpoint.
</success_criteria>

<output>
After completion, create .planning/phases/01-orientation-configuration-contract/01-01-SUMMARY.md
</output>

---
phase: 02-tilt-aware-control-behavior
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/heliostat.h
  - test/test_tilt_control_transform.cpp
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
must_haves:
  truths:
    - Tracking applies mount-orientation transform before actuator target computation.
    - With zero tilt, reflected actuator targets remain equivalent to pre-transform level-install behavior.
    - Orientation updates in controller state affect subsequent reflection outputs without restart.
  artifacts:
    - path: src/heliostat.h
      provides: reflect path wiring that applies mount-orientation transform before bisector-to-spherical conversion.
    - path: test/test_tilt_control_transform.cpp
      provides: Reflection-path regression tests proving transform placement and runtime state consumption.
  key_links:
    - from: src/heliostat.h
      to: src/geometry.cpp
      via: reflect() calls applyMountOrientationTransform on source and target vectors before bisector
      pattern: reflect\(|applyMountOrientationTransform
    - from: src/heliostat.h
      to: HeliostatController runtime state
      via: tiltDeg and tiltAzimuthDeg read on each reflect invocation
      pattern: tiltDeg|tiltAzimuthDeg
---

<objective>
Integrate mount-orientation transform into heliostat reflection control flow so tracking outputs are tilt-aware while remaining backward compatible at zero tilt.

Purpose: Deliver runtime control correctness for tilted installs without changing API/persistence contracts.
Output: Updated `reflect(...)` control math and regression tests covering transform placement and live-state behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tilt-aware-control-behavior/02-CONTEXT.md
@.planning/phases/02-tilt-aware-control-behavior/02-RESEARCH.md
@.planning/phases/02-tilt-aware-control-behavior/02-01-SUMMARY.md
@src/heliostat.h
@src/geometry.h
@test/test_tilt_control_transform.cpp

<interfaces>
From src/heliostat.h:
```cpp
SphericalCoordinate reflect(SphericalCoordinate source, SphericalCoordinate target)
void reflectCurrentSource()
```

From src/geometry.h:
```cpp
vec3 toCartesian(vec2 spherical);
vec2 toSpherical(vec3 cartesian);
vec3 applyMountOrientationTransform(vec3 input, double tiltDeg, double tiltAzimuthDeg);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reflection-path tests that prove transform placement before target computation</name>
  <read_first>
    - src/heliostat.h
    - test/test_tilt_control_transform.cpp
    - .planning/phases/02-tilt-aware-control-behavior/02-RESEARCH.md
  </read_first>
  <files>test/test_tilt_control_transform.cpp</files>
  <behavior>
    - Test 1: Reflection output changes when non-zero orientation is applied to the same source/target inputs.
    - Test 2: Reflection output remains equivalent to legacy math when tiltDeg is zero.
    - Test 3: Changing tiltDeg or tiltAzimuthDeg between consecutive calls changes subsequent reflection output without restart setup.
  </behavior>
  <action>Extend `test/test_tilt_control_transform.cpp` with reflection-focused cases that compare expected outcomes from transformed source/target vectors before bisector computation. Include a consecutive-call test that mutates orientation parameters between calls and asserts output changes immediately, proving live-state consumption.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main</automated>
  </verify>
  <acceptance_criteria>New tests fail against current reflect implementation (pre-integration) and clearly indicate transform-placement or live-state mismatch.</acceptance_criteria>
  <done>RED regression coverage exists for CTRL-01 and runtime update behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire orientation transform into HeliostatController::reflect using live state each call</name>
  <read_first>
    - src/heliostat.h
    - src/geometry.h
    - src/geometry.cpp
    - test/test_tilt_control_transform.cpp
  </read_first>
  <files>src/heliostat.h</files>
  <behavior>
    - Transform is applied to both source and target vectors before bisector math.
    - Zero-tilt path preserves equivalent actuator target output.
    - reflect() reads current controller tilt fields per invocation, not cached startup values.
  </behavior>
  <action>Update `HeliostatController::reflect(...)` to convert source/target spherical coordinates to cartesian vectors, apply `applyMountOrientationTransform` to each using `tiltDeg` and `tiltAzimuthDeg`, then compute bisector and spherical output exactly as existing flow expects. Keep `reflectCurrentSource()` and control cadence unchanged. Do not modify service/router/persistence contracts in this plan.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main</automated>
  </verify>
  <acceptance_criteria>Reflection-path tests pass and code shows transform occurs before target computation with no new API contract changes.</acceptance_criteria>
  <done>CTRL-01 behavior is implemented in the primary control math path consumed by run-loop and sequencer flows.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run phase-level verification commands for control integration</name>
  <read_first>
    - .planning/phases/02-tilt-aware-control-behavior/02-VALIDATION.md
    - src/heliostat.h
    - test/test_tilt_control_transform.cpp
  </read_first>
  <files>src/heliostat.h, test/test_tilt_control_transform.cpp</files>
  <action>Run full Phase 2 automated verification set in sequence: `python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main`, then `python3 -m platformio run -e esp32-s3-devkitc-1`. Record command outputs in the phase summary with explicit mention of CTRL-01/CTRL-02 evidence and any environment fallback usage.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main && python3 -m platformio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>Build and test commands complete successfully, and summary evidence maps passing checks back to CTRL-01 and CTRL-02.</acceptance_criteria>
  <done>Phase-level verification evidence is ready for `/gsd-verify-work` handoff.</done>
</task>

</tasks>

<verification>
Control math integration succeeds without cadence/service-contract regressions, and automated checks prove transform placement and zero-tilt compatibility.
</verification>

<success_criteria>
- CTRL-01 satisfied by transform application before actuator target computation inside `reflect(...)`.
- CTRL-02 satisfied by zero-tilt equivalence in reflection outputs and unchanged behavior for level installs.
- Runtime orientation changes affect subsequent reflections without restart because reflect reads live state each call.
</success_criteria>

<output>
After completion, create .planning/phases/02-tilt-aware-control-behavior/02-02-SUMMARY.md
</output>

---
phase: 02-tilt-aware-control-behavior
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/geometry.h
  - src/geometry.cpp
  - test/test_orientation_main.cpp
  - test/test_tilt_control_transform.cpp
  - test/test_tilt_zero_equivalence.cpp
autonomous: true
requirements:
  - CTRL-02
must_haves:
  truths:
    - Zero-tilt orientation applies an identity transform to control-frame vectors.
    - Non-zero tilt orientation produces deterministic transformed vectors using locked downslope azimuth semantics.
    - Transform behavior is covered by automated tests compiled in the project PlatformIO harness.
  artifacts:
    - path: src/geometry.h
      provides: Mount-orientation transform function declarations with degree-based inputs.
    - path: src/geometry.cpp
      provides: Transform implementation with zero-tilt identity fast path and internal azimuth normalization.
    - path: test/test_tilt_zero_equivalence.cpp
      provides: Unit coverage for zero-tilt equivalence and azimuth wrap handling.
    - path: test/test_tilt_control_transform.cpp
      provides: Unit coverage for non-zero tilt transform direction and sign behavior.
  key_links:
    - from: test/test_tilt_zero_equivalence.cpp
      to: src/geometry.cpp
      via: Direct helper invocation for identity assertions
      pattern: applyMountOrientationTransform
    - from: test/test_tilt_control_transform.cpp
      to: src/geometry.cpp
      via: Cardinal azimuth transform assertions
      pattern: tiltAzimuthDeg|degToRad
---

<objective>
Define and lock mount-orientation transform behavior with a RED-GREEN test cycle before controller integration.

Purpose: Establish deterministic geometry contracts that guarantee zero-tilt compatibility and prevent sign/order regressions.
Output: New geometry helper APIs plus automated test modules for transform and zero-tilt equivalence behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tilt-aware-control-behavior/02-CONTEXT.md
@.planning/phases/02-tilt-aware-control-behavior/02-RESEARCH.md
@.planning/phases/02-tilt-aware-control-behavior/02-VALIDATION.md
@src/geometry.h
@src/geometry.cpp
@test/test_orientation_main.cpp

<interfaces>
From src/geometry.h:
- vec3 toCartesian(vec2 spherical)
- vec2 toSpherical(vec3 cartesian)
- struct SphericalCoordinate { double azimuth; double elevation; }

Contract to add in this plan:
- vec3 applyMountOrientationTransform(vec3 input, double tiltDeg, double tiltAzimuthDeg)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing transform contract tests for zero-tilt and cardinal downslope cases</name>
  <read_first>
    - .planning/phases/02-tilt-aware-control-behavior/02-CONTEXT.md
    - .planning/phases/02-tilt-aware-control-behavior/02-VALIDATION.md
    - test/test_orientation_main.cpp
  </read_first>
  <files>test/test_orientation_main.cpp, test/test_tilt_control_transform.cpp, test/test_tilt_zero_equivalence.cpp</files>
  <behavior>
    - Test 1: tiltDeg=0 returns unchanged vector components within strict tolerance.
    - Test 2: tiltAzimuthDeg values 0 and 360 produce equivalent transformed outputs.
    - Test 3: representative cardinal downslope azimuth values produce deterministic transformed vector direction changes.
  </behavior>
  <action>Create `test/test_tilt_zero_equivalence.cpp` and `test/test_tilt_control_transform.cpp`, register their test functions in `test/test_orientation_main.cpp`, and write assertions against the planned `applyMountOrientationTransform` geometry helper. Execute `python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main` and confirm build fails first because helper symbols are not implemented yet.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main</automated>
  </verify>
  <acceptance_criteria>New tests are compiled into orientation test target and initially fail at compile/link stage due to missing transform helper declarations or definitions.</acceptance_criteria>
  <done>RED stage complete with failing tests that explicitly define transform contract behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement geometry transform helper with exact zero-tilt identity path</name>
  <read_first>
    - src/geometry.h
    - src/geometry.cpp
    - test/test_tilt_zero_equivalence.cpp
    - test/test_tilt_control_transform.cpp
  </read_first>
  <files>src/geometry.h, src/geometry.cpp</files>
  <behavior>
    - Test 1: Input vector returns exactly unchanged when tiltDeg is zero.
    - Test 2: Internal azimuth normalization preserves external contract semantics while accepting 360.
    - Test 3: Non-zero tilt rotates vectors according to downslope azimuth convention.
  </behavior>
  <action>Add `applyMountOrientationTransform(vec3 input, double tiltDeg, double tiltAzimuthDeg)` to geometry helpers. Keep external parameters in degrees, convert radians internally, normalize azimuth internally only, and return input immediately for zero-tilt to preserve compatibility. Implement with existing vec3 rotation helpers only (no new dependencies), and avoid changing existing public REST/state contract fields.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main</automated>
  </verify>
  <acceptance_criteria>All new transform tests compile in build-only PlatformIO harness and pass assertions for identity, azimuth wrap equivalence, and cardinal downslope transform behavior.</acceptance_criteria>
  <done>GREEN stage complete with deterministic transform helper behavior locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor test helper tolerances and enforce repeatable transform assertions</name>
  <read_first>
    - test/test_tilt_zero_equivalence.cpp
    - test/test_tilt_control_transform.cpp
    - .planning/phases/02-tilt-aware-control-behavior/02-VALIDATION.md
  </read_first>
  <files>test/test_tilt_zero_equivalence.cpp, test/test_tilt_control_transform.cpp</files>
  <behavior>
    - Test utilities compare vectors with explicit numeric tolerance without masking sign mistakes.
    - Zero-tilt equivalence checks remain strict enough to catch accidental trig-path drift.
  </behavior>
  <action>Add shared local assertion helpers in the two new test files for component-wise comparisons and azimuth wrap handling. Keep tolerance explicit in test code comments/constants and ensure no redundant trig conversion logic is copied into tests beyond expected-value setup.</action>
  <verify>
    <automated>python3 -m platformio test -e esp32-s3-devkitc-1 --without-uploading --without-testing --filter test_orientation_main && python3 -m platformio run -e esp32-s3-devkitc-1</automated>
  </verify>
  <acceptance_criteria>Tests are readable, deterministic, and compile cleanly with no warnings introduced by helper assertions.</acceptance_criteria>
  <done>REFACTOR stage complete with stable transform contract tests ready for controller integration.</done>
</task>

</tasks>

<verification>
Geometry transform contract is test-defined first and then implemented with build-only PlatformIO verification plus full firmware build green.
</verification>

<success_criteria>
- CTRL-02 is satisfied at geometry-contract level with explicit zero-tilt identity proof.
- Transform helper behavior is deterministic across cardinal downslope azimuth scenarios.
- New tests compile and run in the existing orientation test harness without adding dependencies.
</success_criteria>

<output>
After completion, create .planning/phases/02-tilt-aware-control-behavior/02-01-SUMMARY.md
</output>

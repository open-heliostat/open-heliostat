---
phase: 03-setup-ui-and-verification-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - interface/package.json
  - interface/vitest.config.ts
  - interface/src/test/setup.ts
  - interface/src/routes/heliostat/heliostat-orientation.test.ts
  - interface/src/routes/heliostat/Heliostat.svelte
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - User can see a dedicated mount orientation setup section on the heliostat page.
    - User can edit tilt magnitude and tilt direction in degrees with immediate inline validation.
    - Invalid orientation values are blocked from apply and clearly explained in-page.
  artifacts:
    - path: interface/src/routes/heliostat/Heliostat.svelte
      provides: Dedicated mount orientation setup card, draft state, and inline validation behavior.
    - path: interface/src/routes/heliostat/heliostat-orientation.test.ts
      provides: Automated coverage for dedicated section rendering and validation behavior.
    - path: interface/vitest.config.ts
      provides: Route-level Svelte test runner configuration.
  key_links:
    - from: interface/src/routes/heliostat/Heliostat.svelte
      to: src/orientation_contract.h
      via: mirrored tilt bounds and degree semantics
      pattern: tiltDeg.*-90.*90|tiltAzimuthDeg.*0.*360
    - from: interface/src/routes/heliostat/heliostat-orientation.test.ts
      to: interface/src/routes/heliostat/Heliostat.svelte
      via: component interaction tests for section and validation
      pattern: render\(Heliostat\)|fireEvent
---

<objective>
Establish the dedicated orientation setup surface with reliable validation and fast automated feedback.

Purpose: Deliver UI-01 with explicit orientation controls and inline guardrails that match the firmware contract.
Output: Test harness files plus a dedicated orientation setup section in the existing heliostat route.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-setup-ui-and-verification-loop/03-CONTEXT.md
@.planning/phases/03-setup-ui-and-verification-loop/03-RESEARCH.md
@src/orientation_contract.h
@interface/src/routes/heliostat/Heliostat.svelte
</read_first>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/02-tilt-aware-control-behavior/02-02-SUMMARY.md

<interfaces>
From src/orientation_contract.h:
```cpp
constexpr double kMinTiltDeg = -90.0;
constexpr double kMaxTiltDeg = 90.0;
constexpr double kMinTiltAzimuthDeg = 0.0;
constexpr double kMaxTiltAzimuthDeg = 360.0;
```

From interface/src/routes/heliostat/Heliostat.svelte:
```ts
type HeliostatControllerState = {
  enabled: boolean;
  currentSource: string;
  currentTarget: string;
  sourcesMap: { [key: string]: Direction };
  sunTracker: {
    latitude: number;
    longitude: number;
    isTimeSet: boolean;
    azimuth: number;
    elevation: number;
    timestamp?: number;
    utcIso?: string;
    localIso?: string;
    tz?: string;
    offsetMinutes?: number;
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap route-level UI test harness for heliostat setup</name>
  <files>interface/package.json, interface/vitest.config.ts, interface/src/test/setup.ts</files>
  <action>Add `test` and `test:run` scripts to `interface/package.json`. Install and configure `vitest`, `@testing-library/svelte`, and `jsdom` for Svelte route tests. Create `interface/vitest.config.ts` and `interface/src/test/setup.ts` with deterministic fetch mocking and DOM setup. Keep scope limited to heliostat route tests; do not modify firmware test infrastructure.</action>
  <verify>
    <automated>cd interface && npm run test:run -- --passWithNoTests</automated>
  </verify>
  <done>Vitest executes in the interface workspace with jsdom and can run route-level tests from CLI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedicated mount orientation setup section with inline validation</name>
  <files>interface/src/routes/heliostat/Heliostat.svelte, interface/src/routes/heliostat/heliostat-orientation.test.ts</files>
  <behavior>
    - Test 1: Mount Orientation section renders separately from source/target and sun tracker sections.
    - Test 2: `tiltDeg` enforces [-90, 90] and shows inline validation message when invalid.
    - Test 3: `tiltAzimuthDeg` enforces [0, 360] and shows inline validation message when invalid.
    - Test 4: Apply action is disabled while validation errors exist.
  </behavior>
  <action>Extend the route state typing to include `mountOrientation` and introduce orientation draft fields for edit safety. Add a dedicated SettingsCard section in `Heliostat.svelte` using existing `GridForm`, `Slider`, and `Button` components. Implement immediate inline validation text near controls and wire a single section-level Apply button state to validation result and dirty state. Keep this plan focused on UI-01 and do not add apply/read-back observation messaging yet (that belongs to Plan 03-02).</action>
  <verify>
    <automated>cd interface && npm run test:run -- src/routes/heliostat/heliostat-orientation.test.ts && npm run check</automated>
  </verify>
  <done>User can edit orientation in a dedicated setup section with inline validation and blocked apply on invalid values.</done>
</task>

</tasks>

<verification>
Run: `cd interface && npm run lint && npm run check && npm run test:run -- src/routes/heliostat/heliostat-orientation.test.ts`
</verification>

<acceptance_criteria>
- Dedicated mount orientation setup section is present in `Heliostat.svelte` and visually separated from source/target and sun tracker controls.
- Both orientation inputs are degree-based, bounded, and inline-validated in-page.
- Automated test suite includes route-level UI-01 coverage and passes in CI-capable CLI execution.
</acceptance_criteria>

<success_criteria>
UI-01 is complete and verified: user can configure orientation in a dedicated section with immediate inline validation feedback and stable automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-ui-and-verification-loop/03-01-SUMMARY.md`
</output>

---
phase: 03-setup-ui-and-verification-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - interface/src/routes/heliostat/Heliostat.svelte
  - interface/src/routes/heliostat/heliostat-apply-loop.test.ts
autonomous: false
requirements:
  - UI-02
must_haves:
  truths:
    - User can explicitly apply orientation changes instead of implicit background mutation.
    - After apply, user sees refreshed orientation and sun-tracker observations in the same page context.
    - Refreshing the page reloads persisted orientation values from `/rest/heliostat`.
  artifacts:
    - path: interface/src/routes/heliostat/Heliostat.svelte
      provides: Apply-readback-observe loop and refresh-aware orientation hydration behavior.
    - path: interface/src/routes/heliostat/heliostat-apply-loop.test.ts
      provides: Automated coverage for apply action and read-back synchronization.
  key_links:
    - from: interface/src/routes/heliostat/Heliostat.svelte
      to: /rest/heliostat
      via: explicit `postJsonRest` apply call followed by `getHeliostatControllerState`
      pattern: postJsonRest\(restPath.*mountOrientation|getHeliostatControllerState\(\)
    - from: interface/src/routes/heliostat/Heliostat.svelte
      to: sunTracker display block
      via: in-page observation panel updates after read-back
      pattern: sunTracker\.azimuth|sunTracker\.elevation
---

<objective>
Complete the operator verification loop so orientation changes can be applied, observed immediately, and confirmed after refresh.

Purpose: Deliver UI-02 with explicit apply-readback-observe flow bound to the existing REST contract.
Output: Apply loop behavior, automated route-level checks, and a human verification gate for real device confirmation.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-setup-ui-and-verification-loop/03-CONTEXT.md
@.planning/phases/03-setup-ui-and-verification-loop/03-RESEARCH.md
@.planning/phases/03-setup-ui-and-verification-loop/03-01-PLAN.md
@interface/src/routes/heliostat/Heliostat.svelte
</read_first>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-tilt-aware-control-behavior/02-02-SUMMARY.md

<interfaces>
From interface/src/lib/stores/rest.ts:
```ts
export async function getJsonRest<T>(path: string, data: T)
export async function postJsonRest<T>(path: string, data: T)
```

From src/HeliostatService.cpp (existing REST payload contract):
```cpp
{"mountOrientation", [&](HeliostatController &controller, JsonVariant content) {
  JsonObject obj = content.to<JsonObject>();
  OrientationContract::writeMountOrientation(obj, controller.tiltDeg, controller.tiltAzimuthDeg);
}},
{"sunTracker", [&](HeliostatController &controller, JsonVariant content) {
  obj["azimuth"] = controller.getSolarPosition().azimuth;
  obj["elevation"] = controller.getSolarPosition().elevation;
}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement apply-readback-observe loop for orientation setup</name>
  <files>interface/src/routes/heliostat/Heliostat.svelte, interface/src/routes/heliostat/heliostat-apply-loop.test.ts</files>
  <behavior>
    - Test 1: Clicking Apply posts only mountOrientation payload and then performs a follow-up GET refresh.
    - Test 2: After apply/read-back, displayed orientation values match backend response values.
    - Test 3: Observation block shows updated sun tracker azimuth/elevation after refresh.
    - Test 4: Initial load and browser refresh hydrate orientation controls from `/rest/heliostat` response.
  </behavior>
  <action>Add a dedicated orientation apply handler that posts `{ mountOrientation: { tiltDeg, tiltAzimuthDeg } }` and then immediately calls `getHeliostatControllerState()`. Update the section to display last-apply status text (success/error) inline and keep verification context adjacent to current sun tracker values. Ensure orientation draft state is rehydrated from backend data on mount and after apply so page refresh reflects persisted values. Keep this implementation inside `Heliostat.svelte` and do not introduce a new global store or toast system.</action>
  <verify>
    <automated>cd interface && npm run test:run -- src/routes/heliostat/heliostat-apply-loop.test.ts && npm run check && npm run build</automated>
  </verify>
  <done>User can run configure -> apply -> observe in-page and values are sourced from refreshed backend state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm real-device apply-test-observe-refresh loop</name>
  <files>interface/src/routes/heliostat/Heliostat.svelte</files>
  <action>Run a real-device verification pass of the implemented apply/read-back workflow to confirm that backend persistence and observation behavior remain correct outside mocked tests.</action>
  <what-built>Orientation setup section with explicit apply, immediate REST read-back, and in-page observation signals.</what-built>
  <how-to-verify>
    1. Open `/heliostat` on the target device UI.
    2. Set `tiltDeg=15` and `tiltAzimuthDeg=225`, then click Apply.
    3. Confirm orientation values remain at 15 and 225 after apply and the observation area updates sun tracker azimuth/elevation values.
    4. Refresh the browser page.
    5. Confirm orientation controls still show 15 and 225 from `/rest/heliostat` persisted state.
  </how-to-verify>
  <verify>
    <automated>cd interface && npm run test:run -- src/routes/heliostat/heliostat-apply-loop.test.ts && npm run check</automated>
  </verify>
  <done>Human verification confirms apply-observe-refresh behavior on hardware and reports either approval or concrete mismatch details.</done>
  <resume-signal>Type "approved" to continue, or share observed mismatch details.</resume-signal>
</task>

</tasks>

<verification>
Run: `cd interface && npm run lint && npm run check && npm run build && npm run test:run -- src/routes/heliostat/heliostat-orientation.test.ts src/routes/heliostat/heliostat-apply-loop.test.ts`
</verification>

<acceptance_criteria>
- Orientation changes require explicit Apply action and are not silently persisted on control edits.
- Apply action performs immediate backend re-read and updates in-page orientation and observation values.
- Browser refresh confirms persisted orientation values are reloaded and shown in the setup section.
- Human verification checkpoint instructions are executable on real hardware.
</acceptance_criteria>

<success_criteria>
UI-02 is complete and verified: user can execute a reliable configure-apply-observe-refresh loop to validate orientation effects.
</success_criteria>

<output>
After completion, create `.planning/phases/03-setup-ui-and-verification-loop/03-02-SUMMARY.md`
</output>
